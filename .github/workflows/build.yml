on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - run: node --version
      - run: npm  --version
on: push
jobs:
  second-job:
    runs-on: ubuntu-latest
    steps:
      - run: git --version
      
